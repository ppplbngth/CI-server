package group22;

import org.junit.jupiter.api.Test;
import static org.junit.Assert.assertTrue;

/**
 * class for testing automatedTestProject.java
 */
public class AutomatedTest {
    
    /**
     * Test for testing that the AutomatedTestProject works properly.
     * The testBranch should run the tests from the repo. If it runs the tests
     * it means it is successful.
     */
    @Test
    public void runsMvnCommandTest(){
        Boolean test = false;
        assertTrue(test);
    }
}
